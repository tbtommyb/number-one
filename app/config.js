// Use Heroku environment variables when deploying this to keep them hidden
// Change mongodb to SQL
// HTTP basic auth to get in user's login details

module.exports = {
	'secret': 'randomaccess',
	'database': 'mongodb://localhost'
};